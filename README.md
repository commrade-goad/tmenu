# TMENU
a simple TUI scriptable menu similar to `dmenu`, `fzf` and all that stuff but in C and simpler without too much feature.

## Dependency
- readline
- ncurses

## Build
```sh
make
```

> __NOTE:__ Example usage can be seen on the `./runner` script.
